"""Tiny deterministic demo for renewable-forecasting-benchmark; replace synthetic values with licensed data."""
from pathlib import Path
print({"project": "renewable-forecasting-benchmark", "status": "baseline ready", "next": "add documented dataset"})
