[project]
name = "renewable_forecasting_benchmark"
version = "0.1.0"
description = "Reproducible wind and solar forecasting benchmarks with leakage-safe evaluation protocols."
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "pandas>=2.0", "scikit-learn>=1.3"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
